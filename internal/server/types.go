package server

import "vexlo/internal/model"

type RoutingRule = model.RoutingRule
type Session = model.Session
type CapturedRequest = model.CapturedRequest
type CapturedReplay = model.CapturedReplay
